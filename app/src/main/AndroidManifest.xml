<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nl.martijndorsman.imtpmd">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:screenOrientation="portrait">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VakkenlijstActivity">
        </activity>
        <activity android:name=".PopSpinner"
        android:theme= "@style/AppTheme.PopTheme">
    </activity>
        <activity android:name=".KeuzevakPopup"
            android:theme= "@style/AppTheme.PopTheme">
        </activity>
        <activity android:name=".VoortgangActivity">
        </activity>
    </application>

</manifest>